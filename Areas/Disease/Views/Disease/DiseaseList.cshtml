@{
    ViewData["Title"] = "DiseaseList";
    Layout = "~/Views/Shared/_Layout.cshtml";

}
@using System.Data
@model DataTable




<div class="content-wrapper">
    <h4 class="fw-bold py-3 mb-1"><span class="text-muted fw-light">Tables /</span> Basic Tables</h4>
    <!-- Content -->
    <div class="container-xxl flex-grow-1 container-p-y">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-10">
                    <h3>Disease List</h3>
                </div>

                <div class="col-md-2">
                    <a asp-controller="Disease" asp-action="Add" class="btn btn-success">Add Disease</a>
                </div>
            </div>
            <hr />

        </div>

        <!-- Basic Bootstrap Table -->
        <div class="card">
            <div class="table-responsive text-nowrap">
                <table class="table">
                    <thead>
                        <tr>
                            <th>DiseaseName</th>
                            <th>Created</th>
                            <th>Modified</th>
                        </tr>

                    </thead>
                    <tbody class="table-border-bottom-0">
                        @foreach (DataRow dr in Model.Rows)
                        {
                            <tr>
                                <td>@dr["DiseaseName"]</td>
                                <td>@Convert.ToDateTime(dr["Created"]).ToString("dd-mm-yyyy")</td>
                                <td>@Convert.ToDateTime(dr["Modified"]).ToString("dd-mm-yyyy")</td>
                                <td>
                                    <form method="post" asp-area="Disease" asp-controller="Disease" asp-action="Delete">
                                        <input type="hidden" name="DiseaseID" value="@Convert.ToInt32(dr["DiseaseID"])">
                                        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this?')">Delete</button>
                                        <a class="btn btn-danger" asp-area="Disease" asp-controller="Disease" asp-action="Add" asp-route-Diseaseid="@Convert.ToInt32(dr["DiseaseID"])">Update</a>
                                    </form>
                                </td>


                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!--/ Basic Bootstrap Table -->
    </div>
    </div>
